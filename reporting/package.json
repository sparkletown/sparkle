{
  "name": "reporting",
  "description": "Tool to fetch zoom reports",
  "scripts": {
    "lint": "npm run prettier:check",
    "prettier": "echo Error: Run prettier from the root of the project directory. && exit 1",
    "prettier:check": "echo Error: Run prettier from the root of the project directory. && exit 1"
  },
  "dependencies": {
    "puppeteer": "^3.3.0"
  },
  "private": true,
  "devDependencies": {
    "@types/puppeteer": "^5.4.4"
  }
}
